{
  "name": "data-umsa",
  "version": "1.0.0",
  "description": "landing page \"DATA UMSA\"",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && parcel build ./public/index.html --no-source-maps",
    "start": "parcel ./public/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.13.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.13.3",
    "svgo": "^3.3.2"
  }
}
